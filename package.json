{
  "name": "hillstation-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\" \"npm run dev --prefix admin\"",
    "start": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\" \"npm run dev --prefix admin\"",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
